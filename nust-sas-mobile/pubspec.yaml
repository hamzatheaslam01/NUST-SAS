name: nust_sas_mobile
description: NUST Secure Attendance System Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5
  google_mlkit_face_detection: ^0.10.0
  google_mlkit_barcode_scanning: ^0.12.0
  geolocator: ^10.1.0
  device_info_plus: ^9.1.0
  flutter_secure_storage: ^9.0.0
  dio: ^5.3.0
  qr_flutter: ^4.1.0
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
